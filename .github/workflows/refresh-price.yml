name: Refresh MON price

# Pings /api/cron/refresh-price every 10 minutes so the DB-stored MON price
# (read by network/overview, income, MEV, simulator, reports) tracks live
# market price within ~10 min. Previously ran every 2 min — slowed down to
# every 10 min to stay under GitHub Actions free-tier minutes.

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Hit refresh-price endpoint
        run: |
          set -euo pipefail
          URL="https://monad-income-tracker.vercel.app/api/cron/refresh-price"
          AUTH=""
          if [ -n "${CRON_SECRET:-}" ]; then
            AUTH="-H \"Authorization: Bearer ${CRON_SECRET}\""
          fi
          # shellcheck disable=SC2086
          eval curl -sS -w '\nHTTP %{http_code}\n' --max-time 30 $AUTH "$URL"
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
